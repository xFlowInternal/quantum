version: "3.9"

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "9091:9091"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_WS_URL: ""   # empty = use relative /ws (proxied by nginx)
    ports:
      - "9092:9092"
    depends_on:
      - backend
    restart: unless-stopped
